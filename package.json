{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "A description of your project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node script.js" // Update or add other scripts as necessary
  },
  "dependencies": {
    "express": "^4.17.1",
    "axios": "^0.21.1",
    "dotenv": "^10.0.0",
    "body-parser": "^1.19.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16" // Specify the minimum version of Node.js. Use "node" for the latest version
  }
}
